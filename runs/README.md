# Run Records

Small preflight reports and final run manifests belong in this directory.

Do not commit raw generations, prepared prompts, checkpoints, directions, or large benchmark outputs. Store their SHA-256 hashes and storage locations in a small manifest instead.
